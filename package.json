{
  "name": "payment-recommender-mono",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["api", "web"],
  "scripts": {
    "api:dev": "cd api && npm run dev",
    "web:dev": "python3 -m http.server 4173 --directory web",
    "test:api": "cd api && npm test"
  }
}
